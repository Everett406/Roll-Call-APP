name: roll_call_app
description: 点到为止 - 让点名更有诗意
publish_to: 'none'
version: 1.1.5+5

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  hive_flutter: ^1.1.0
  uuid: ^4.3.3
  intl: ^0.19.0
  dynamic_color: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
